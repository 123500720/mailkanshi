# Windows launcher scripts must use CRLF so cmd.exe parses them correctly.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Keep normal text files with LF in the repo.
*.py text eol=lf
*.md text eol=lf
